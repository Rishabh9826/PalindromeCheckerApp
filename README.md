PalindromeChecker App 🎯 Objective

The PalindromeChecker App checks whether a given word, number, or sentence reads the same forward and backward.

🔄 Project Flow

User enters input (word/number/sentence).

App converts input to lowercase and removes spaces (optional: special characters).

Input is reversed.

Original and reversed values are compared.

Result is displayed (Palindrome / Not Palindrome).

💡 Use Case

Beginner programming practice

Understanding string manipulation

Coding interview preparation

Integration into text-based applications

